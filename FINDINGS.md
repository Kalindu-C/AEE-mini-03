* I analyse both Scrapy and Playwright for Crawling task, but found Playwright is good for dynamic web pages and Scrapy is good for the large scale static web pages  

* Playwright is a testing framework, but the same engine they used for testing we can use it for the crawling

* i got error when installing pytest-playwright package beacuse it initially asked for pip, but i created venv using uv it does not intall pip intensionally, that why i got that error. so right command is that
uv pip install pytest-playwright

